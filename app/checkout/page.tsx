import Header from '@/components/Header';
import CheckoutContent from '@/components/CheckoutContent';

export const metadata = { title: 'Commande — Hestia' };

export default function CheckoutPage() {
  return (
    <>
      <Header />
      <CheckoutContent />
    </>
  );
}
